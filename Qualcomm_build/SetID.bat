set ID=M1.1.1-Zeus_002082

for /f "tokens=1-4 delims=_" %%a in ("%ID%") do (set Temp1=%%b& set Temp2=%%c)

if "xx%Temp2%"=="xx" (
set PRJID=%Temp1%
) else (
set PRJID=%Temp2%
)

if "%PRJID%"== "Zeus" (
for /f "tokens=1-4 delims=M" %%a in ("%ID%") do (set RDID=R%%a-%username%)
) else (
for /f "tokens=1-4 delims=M" %%a in ("%ID%") do (set RDID=R%%aM%%b-%username%)
)

echo #define QQ "%RDID%"; > %TASK%\version.h


if /I "%username%"=="Jeff3_Lin" (
echo #define QQ "%ID%"; > %TASK%\version.h

 )

if /I "%username%"=="Alice_Chou" (
echo #define QQ "%ID%"; > %TASK%\version.h

 )

if /I "%username%"=="Eric7_Chen" (
echo #define QQ "%ID%"; > %TASK%\version.h

 )












































































































































































































































































































































































































































































































































































































































































